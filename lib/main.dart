import 'package:flutter/material.dart';

void main() {
  runApp(  SimpleCode());
}
class SimpleCode extends StatelessWidget {
  const SimpleCode({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child:Text('Code')
    );
  }
}

 
